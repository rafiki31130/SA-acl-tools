# Macros shipped by SA-acl-tools.
#
# `acl_inventory` / `acl_inventory(1)` : inventory of the knowledge objects, built on
# the NATIVE ENDPOINTS and not on `admin/directory` (specification section 6.7).
# `editacl_rollback(1)`                : rollback set of one execution, as a
#                                        PREVIEW (section 8.6).
# `editacl_rollback_apply(1)`          : the same, followed by the complete and
#                                        correctly quoted `| editacl` invocation. It
#                                        WRITES (D-13).
#
# The two inventory forms share a single implementation, `acl_inventory_base`, which is
# not meant to be invoked directly.
#
# Why not simply use `| rest ... /admin/directory`: that aggregation handler only sees
# a fraction of the objects (60.6 % on the reference platform), emits no lookup file
# and no data model, and 100 % of the `id` values it produces are self-referential,
# hence useless for addressing the object.

[acl_inventory]
definition = `acl_inventory_base("")`
description = Complete inventory of the knowledge objects, all families, built on the \
native endpoints. Emits exactly the input contract of `editacl`: title, \
eai:acl.app, eai:acl.owner, eai:acl.perms.read, eai:acl.perms.write, eai:acl.sharing, \
eai:type, id. Usage: | `acl_inventory` | search ... | editacl ...
iseval = 0

# Parameterized form: `acl_inventory(<family>[,<family>...])`.
#
# The family names are the keys of the mapping table, that is, the `family` column of
# the `acl_object_families` lookup. Only the endpoints of the requested families are
# queried: that is the cost lever for interactive use (section 6.7 constraint 2).
#
# Usage: | `acl_inventory(savedsearch,views)` | search ... | editacl ...
#
# Splunk indexes macros by ARITY: `acl_inventory(savedsearch,views)` is a TWO-argument
# call and looks for the `[acl_inventory(2)]` stanza, not for `[acl_inventory(1)]` with
# a "savedsearch,views" string. The parameterized form of the specification therefore
# requires one stanza per argument count. The ceiling is the number of families in the
# shipped lookup (27): beyond that, the complete inventory without an argument is the
# right answer. Each stanza reassembles the list and passes it to `acl_inventory_base`.

[acl_inventory(1)]
args = f1
definition = `acl_inventory_base("$f1$")`
iseval = 0

[acl_inventory(2)]
args = f1, f2
definition = `acl_inventory_base("$f1$,$f2$")`
iseval = 0

[acl_inventory(3)]
args = f1, f2, f3
definition = `acl_inventory_base("$f1$,$f2$,$f3$")`
iseval = 0

[acl_inventory(4)]
args = f1, f2, f3, f4
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$")`
iseval = 0

[acl_inventory(5)]
args = f1, f2, f3, f4, f5
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$")`
iseval = 0

[acl_inventory(6)]
args = f1, f2, f3, f4, f5, f6
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$")`
iseval = 0

[acl_inventory(7)]
args = f1, f2, f3, f4, f5, f6, f7
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$")`
iseval = 0

[acl_inventory(8)]
args = f1, f2, f3, f4, f5, f6, f7, f8
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$")`
iseval = 0

[acl_inventory(9)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$")`
iseval = 0

[acl_inventory(10)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$")`
iseval = 0

[acl_inventory(11)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10, f11
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$,$f11$")`
iseval = 0

[acl_inventory(12)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10, f11, f12
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$,$f11$,$f12$")`
iseval = 0

[acl_inventory(13)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10, f11, f12, f13
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$,$f11$,$f12$,$f13$")`
iseval = 0

[acl_inventory(14)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10, f11, f12, f13, f14
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$,$f11$,$f12$,$f13$,$f14$")`
iseval = 0

[acl_inventory(15)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10, f11, f12, f13, f14, f15
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$,$f11$,$f12$,$f13$,$f14$,$f15$")`
iseval = 0

[acl_inventory(16)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10, f11, f12, f13, f14, f15, f16
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$,$f11$,$f12$,$f13$,$f14$,$f15$,$f16$")`
iseval = 0

[acl_inventory(17)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10, f11, f12, f13, f14, f15, f16, f17
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$,$f11$,$f12$,$f13$,$f14$,$f15$,$f16$,$f17$")`
iseval = 0

[acl_inventory(18)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10, f11, f12, f13, f14, f15, f16, f17, f18
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$,$f11$,$f12$,$f13$,$f14$,$f15$,$f16$,$f17$,$f18$")`
iseval = 0

[acl_inventory(19)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10, f11, f12, f13, f14, f15, f16, f17, f18, f19
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$,$f11$,$f12$,$f13$,$f14$,$f15$,$f16$,$f17$,$f18$,$f19$")`
iseval = 0

[acl_inventory(20)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10, f11, f12, f13, f14, f15, f16, f17, f18, f19, f20
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$,$f11$,$f12$,$f13$,$f14$,$f15$,$f16$,$f17$,$f18$,$f19$,$f20$")`
iseval = 0

[acl_inventory(21)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10, f11, f12, f13, f14, f15, f16, f17, f18, f19, f20, f21
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$,$f11$,$f12$,$f13$,$f14$,$f15$,$f16$,$f17$,$f18$,$f19$,$f20$,$f21$")`
iseval = 0

[acl_inventory(22)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10, f11, f12, f13, f14, f15, f16, f17, f18, f19, f20, f21, f22
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$,$f11$,$f12$,$f13$,$f14$,$f15$,$f16$,$f17$,$f18$,$f19$,$f20$,$f21$,$f22$")`
iseval = 0

[acl_inventory(23)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10, f11, f12, f13, f14, f15, f16, f17, f18, f19, f20, f21, f22, f23
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$,$f11$,$f12$,$f13$,$f14$,$f15$,$f16$,$f17$,$f18$,$f19$,$f20$,$f21$,$f22$,$f23$")`
iseval = 0

[acl_inventory(24)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10, f11, f12, f13, f14, f15, f16, f17, f18, f19, f20, f21, f22, f23, f24
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$,$f11$,$f12$,$f13$,$f14$,$f15$,$f16$,$f17$,$f18$,$f19$,$f20$,$f21$,$f22$,$f23$,$f24$")`
iseval = 0

[acl_inventory(25)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10, f11, f12, f13, f14, f15, f16, f17, f18, f19, f20, f21, f22, f23, f24, f25
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$,$f11$,$f12$,$f13$,$f14$,$f15$,$f16$,$f17$,$f18$,$f19$,$f20$,$f21$,$f22$,$f23$,$f24$,$f25$")`
iseval = 0

[acl_inventory(26)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10, f11, f12, f13, f14, f15, f16, f17, f18, f19, f20, f21, f22, f23, f24, f25, f26
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$,$f11$,$f12$,$f13$,$f14$,$f15$,$f16$,$f17$,$f18$,$f19$,$f20$,$f21$,$f22$,$f23$,$f24$,$f25$,$f26$")`
iseval = 0

[acl_inventory(27)]
args = f1, f2, f3, f4, f5, f6, f7, f8, f9, f10, f11, f12, f13, f14, f15, f16, f17, f18, f19, f20, f21, f22, f23, f24, f25, f26, f27
definition = `acl_inventory_base("$f1$,$f2$,$f3$,$f4$,$f5$,$f6$,$f7$,$f8$,$f9$,$f10$,$f11$,$f12$,$f13$,$f14$,$f15$,$f16$,$f17$,$f18$,$f19$,$f20$,$f21$,$f22$,$f23$,$f24$,$f25$,$f26$,$f27$")`
iseval = 0

# Shared implementation. Do not invoke directly.
#
# Three design points:
#
#  1. The selection is applied BEFORE the `map`, so a family that was not requested
#     costs no REST call at all. An operator who only handles saved searches does not
#     pay for the enumeration of the lookup files (section 6.7 constraint 2).
#  2. The argument is filtered by `replace` against a character allow list before it
#     is injected into a regular expression: the family name is user input, and it
#     must not be able to alter the regex.
#  3. `eai:type` is SYNTHESIZED when the native endpoint does not emit it (section 6.7
#     constraint 4). The value used is the table key matching the family being
#     queried. Without that synthesis the outbound pass would work, since `id` is
#     usable, but the rollback would be impossible: `editacl_rollback` resolves
#     through `eai:type`, and `id` is not journaled. The natively emitted value, when
#     there is one, is preserved as is.
[acl_inventory_base(1)]
args = families
definition = inputlookup acl_object_families \
| eval acl_selection = replace("$families$", "[^A-Za-z0-9_,-]", "") \
| eval acl_family_re = if(acl_selection == "", ".*", "^(" . replace(acl_selection, ",", "|") . ")$") \
| where match(family, acl_family_re) \
| fields family, handler_path \
| map maxsearches=200 search="| rest splunk_server=local count=0 /servicesNS/-/-/$handler_path$ | eval acl_family=\"$family$\"" \
| eval "eai:type" = if(isnull('eai:type') OR 'eai:type' == "", acl_family, 'eai:type') \
| table title, "eai:acl.app", "eai:acl.owner", "eai:acl.perms.read", "eai:acl.perms.write", "eai:acl.sharing", "eai:type", id
description = Shared implementation of `acl_inventory` and `acl_inventory(1)`. \
Do not invoke directly.
iseval = 0

# Rollback set of one execution (section 8.6). The SPL is the one from the
# specification, with the sole addition of the leading `search` keyword, which is
# indispensable for the macro to be invocable in generating position
# (`| `editacl_rollback(<sid>)``).
#
# The macro only restores the objects for which an `outcome` line attests that the
# write did succeed: an object whose POST failed was not modified, and it must not be
# "restored" to a state it never left.
#
# The `earliest(...) BY endpoint` is defense in depth: deduplication by URI already
# rules out intra-execution duplicates, but the explicit ordering protects against an
# aggregation that would mistakenly span several `sid` values.
#
# The time range of the calling search must cover the execution to be restored.
[editacl_rollback(1)]
args = sid
definition = search index=_internal sourcetype=editacl:journal sid="$sid$" \
| eval _restorable = if(phase="outcome" AND status="updated", 1, 0) \
| eventstats max(_restorable) AS restorable BY endpoint \
| search phase="intent" restorable=1 \
| stats earliest(before_perms_read)  AS "eai:acl.perms.read", \
        earliest(before_perms_write) AS "eai:acl.perms.write", \
        earliest(before_sharing)     AS "eai:acl.sharing", \
        earliest(before_owner)       AS "eai:acl.owner", \
        earliest(app)                AS "eai:acl.app", \
        earliest(title)              AS title, \
        earliest(eai_type)           AS "eai:type" \
  BY endpoint \
| eval "eai:acl.perms.read"  = coalesce('eai:acl.perms.read',  ""), \
       "eai:acl.perms.write" = coalesce('eai:acl.perms.write', "") \
| fields title "eai:acl.*" "eai:type"
description = Rollback set (preview) of the execution identified by its `sid`. \
Complete it with | editacl dryrun=f and no other parameter: the macro emits the \
platform's native field names, which the four target values pick up by default. \
`eai:acl.owner` carries the PREVIOUS owner, which the default of `new_owner` applies \
without anything having to be named. To avoid depending on that typing, use \
`editacl_rollback_apply(<sid>)`.
iseval = 0

# The `coalesce(..., "")` on the two permission columns is DEFENSE IN DEPTH, and not
# the fix for an observed defect (section 8.6, D-32).
#
# The risk was real in principle: since the rework of section 3.2, an ABSENT column
# preserves the attribute instead of emptying it. If the journaling chain lost an
# empty permission along the way, the rollback would restore nothing while reporting
# a success.
#
# MEASURED ON 9.4.6, IT DOES NOT LOSE IT, and the measurement was redone by a profile
# distinct from the one that had first made it. A permission serialized as "" in the
# journal is EXTRACTED at indexing time: the field is present, its value is the empty
# string, and a control `isnull()` on a non-existent field establishes that the
# instrument does distinguish "absent" from "present and empty". AND it SURVIVES
# AGGREGATION: the `stats earliest(...) BY endpoint` above, run without this
# `coalesce` and filtered on an object whose two permissions are empty on 100 % of the
# aggregated rows, does emit both columns.
#
# The `coalesce` is kept because it costs one line and protects against a behavior
# that nothing obliges another version of the platform to preserve. It is the same
# caution as the re-validation of the mapping table on the target platform
# (section 6.5).
#
# Version 2.3 of the specification claimed on the contrary that the column
# disappeared. That claim had never been measured; it is false.
#
# `eai:acl.sharing` and `eai:acl.owner` are deliberately NOT treated the same way:
# their empty value does not exist on the platform side, a restorable object always
# carries one, and materializing an empty column there would only turn a correct
# preservation into a rejection.

# APPLIED rollback (section 8.6, D-13). Same rollback set as `editacl_rollback(1)`,
# but carrying the complete `| editacl` invocation.
#
# The name says that it WRITES (`dryrun=f`). `editacl_rollback(1)` remains the preview
# form and the default entry point.
#
# The invocation no longer carries any field-naming parameter. That is the direct
# effect of the input contract rework: the macro emits the platform's native field
# names, and the four target values pick them up by default. The v1.3 class of error,
# an unquoted `fields` list truncated by SPL to its first value and a rollback that
# only restored `perms.read` while reporting a success, is eliminated **by
# construction**: there is no list left to quote.
#
# The ceiling is explicit. The default is ten (D-30), which would interrupt the
# rollback of a larger batch; a rollback covers a set already delimited by the `sid`,
# and the operator decided its volume at the time of the outbound pass.
#
# The definition delegates to `editacl_rollback(1)` rather than copying its SPL: two
# copies of the same pipeline would diverge at the first amendment, and the forgotten
# copy would be the one that writes.
[editacl_rollback_apply(1)]
args = sid
definition = `editacl_rollback($sid$)` \
| editacl dryrun=f max_objects=100000
description = APPLIED rollback of the execution identified by its `sid`: the rollback \
set of `editacl_rollback(<sid>)` followed by the complete | editacl invocation, in \
real write mode (dryrun=f) and with no effective ceiling. The four attributes are \
picked up implicitly, `eai:acl.owner` carrying the previous owner. Preview first with \
`editacl_rollback(<sid>)`.
iseval = 0
