__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Mapping table override: created by the operator, never shipped (D-5).
lookups/acl_endpoint_map_override.csv

# Per-instance local configuration (verify_ssl, journal index, ...).
local/

# Logs produced at run time.
*.log
