# Dependance vendorisee sous `bin/lib/` — une seule, version figee au patch.
#
# Le noyau `acltools` n'a AUCUNE dependance tierce : les appels REST sont ecrits en
# HTTP brut sur `urllib` + `ssl` de la bibliotheque standard, et les tests reposent sur
# `unittest`. Cette ligne est donc la totalite de la surface de licence, d'audit et de
# CVE que l'app traine.
#
# Toute dependance supplementaire est un arbitrage a remonter : dans un depot public,
# chaque paquet vendorise est une surface que l'app porte sans mecanisme de mise a jour.
#
# La version 3.x du SDK exige Python >= 3.13 et n'est donc pas installable sur
# l'interpreteur livre par Splunk Enterprise 9.x. La 2.1.1 est la derniere serie
# compatible.
#
# Licence amont : Apache-2.0 (identique a celle de cette app).

splunk-sdk==2.1.1 --hash=sha256:46300d52f09e0aed7e5962ce2ba08ef54421ffb3a538c6af6164dcbf9f075faa
